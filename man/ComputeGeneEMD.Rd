% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_emd.R
\name{ComputeGeneEMD}
\alias{ComputeGeneEMD}
\title{Compute gene Earth Mover's Distances via a Python backend}
\usage{
ComputeGeneEMD(
  srat,
  common_genes,
  dir.path,
  script_dir = NULL,
  npc = 10,
  K = 10,
  reduction = "dm"
)
}
\arguments{
\item{srat}{A \code{Seurat} object. Must have a normalised \code{RNA} assay.}

\item{common_genes}{Character vector of gene names to include in the
computation (e.g., the output of \code{\link[=SelectCommonGenes]{SelectCommonGenes()}}).}

\item{dir.path}{Character string. Path to the directory where intermediate
and output files will be written.}

\item{script_dir}{Character string or NULL. Path to the directory containing
\code{gene_distance_cal_parallel.py}. If NULL (default), uses the bundled
Python script included with the package.}

\item{npc}{Integer. Number of diffusion map components to use. Default \code{10}.}

\item{K}{Integer. Number of nearest neighbours for the cell graph. Default \code{10}.}

\item{reduction}{Character string. Name of the dimensionality reduction to
use when computing graph distances. Default \code{"dm"}.}
}
\value{
Invisibly returns \code{NULL}. Results are written to \code{dir.path}.
}
\description{
Runs diffusion maps and coarse-graining on a Seurat object, writes
intermediate files to disk, and launches a Python script
(\code{gene_distance_cal_parallel.py}) in the background to compute pairwise
gene EMDs via optimal transport. Results are saved to \code{dir.path} and can
be loaded with \code{\link[=LoadGeneEMD]{LoadGeneEMD()}}.
}
\examples{
\dontrun{
ComputeGeneEMD(srat, common_genes, dir.path = "results/emd/")
}
}
