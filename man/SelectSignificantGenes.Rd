% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_selection.R
\name{SelectSignificantGenes}
\alias{SelectSignificantGenes}
\title{Select significant genes per eigenvector using local FDR}
\usage{
SelectSignificantGenes(V, lfdr_thresh = 0.2, min_genes = 3, max_genes = 50)
}
\arguments{
\item{V}{A numeric matrix with genes as rows and eigenvectors as columns.
Row names should be gene names.}

\item{lfdr_thresh}{Numeric. Local FDR threshold for significance. Default
is \code{0.2}. Automatically capped at the empirical FDR of the first bin.}

\item{min_genes}{Integer. Minimum number of significant genes required in
a direction (positive or negative) to retain them. Default is \code{3}.}

\item{max_genes}{Integer. Maximum number of significant genes to keep per
direction per eigenvector. Default is \code{50}.}
}
\value{
A binary matrix of the same dimensions as \code{V} (genes × eigenvectors),
where \code{1} indicates the gene is significantly loaded on that eigenvector.
}
\description{
For each column (eigenvector) of a gene-by-eigenvector matrix, z-transforms
the loadings (using median/MAD for robustness) and applies \code{locfdr} to
identify significantly loaded genes. Returns a binary indicator matrix.
}
\examples{
\dontrun{
gene_indicator <- SelectSignificantGenes(V, lfdr_thresh = 0.2)
}
}
