% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_selection.R
\name{ClusterGenes}
\alias{ClusterGenes}
\title{Cluster genes using hierarchical clustering and dynamic tree cutting}
\usage{
ClusterGenes(
  gene_dist,
  clustering_method = "average",
  min_gene = 2,
  deepSplit = 1,
  z_thresh = 3
)
}
\arguments{
\item{gene_dist}{A \code{dist} object (or symmetric matrix coercible to one)
of pairwise gene distances.}

\item{clustering_method}{Character. Linkage method passed to \code{\link[=hclust]{hclust()}}.
Default \code{"average"}.}

\item{min_gene}{Integer. Minimum cluster size to retain. Default \code{2}.}

\item{deepSplit}{Integer (0–4). Controls the sensitivity of dynamic tree
cutting; higher values produce more, smaller clusters. Default \code{1}.}

\item{z_thresh}{Numeric. MAD outlier threshold passed to \code{\link[=tighten_modules]{tighten_modules()}}.
Default \code{3}.}
}
\value{
A named factor of cluster assignments (levels re-indexed as
consecutive integers). Genes removed as singletons or outliers are
dropped from the result.
}
\description{
Performs agglomerative hierarchical clustering on a gene distance matrix,
cuts the tree dynamically with \code{dynamicTreeCut::cutreeDynamic}, tightens
clusters by removing outliers with \code{\link[=tighten_modules]{tighten_modules()}}, and drops
singleton/small clusters.
}
\examples{
\dontrun{
gene_partition <- ClusterGenes(as.dist(gene.dist.mat))
}
}
