% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_graph.R
\name{affinity.mtx}
\alias{affinity.mtx}
\title{Build a symmetrized Gaussian affinity matrix}
\usage{
affinity.mtx(dist.mat, K = 10, sigma = NULL, nEV = 30, t = 1)
}
\arguments{
\item{dist.mat}{A square numeric matrix of pairwise distances. Row and
column names should be gene (or sample) identifiers.}

\item{K}{Integer. The bandwidth is set to the distance to the K-th nearest
neighbour. Ignored when \code{sigma} is provided. Default \code{10}.}

\item{sigma}{Numeric or NULL. Fixed kernel bandwidth. If NULL (default),
the bandwidth is determined adaptively per row using the K-th nearest
neighbour distance.}

\item{nEV}{Integer. Kept for API compatibility; not currently used internally.
Default \code{30}.}

\item{t}{Integer. Kept for API compatibility; not currently used internally.
Default \code{1}.}
}
\value{
A symmetric, normalised affinity matrix with the same row/column
names as \code{dist.mat}.
}
\description{
Converts a pairwise distance matrix into a symmetrised Gaussian kernel
affinity matrix, then applies a symmetric normalisation (like a normalised
graph Laplacian step).
}
\keyword{internal}
