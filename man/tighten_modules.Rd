% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_selection.R
\name{tighten_modules}
\alias{tighten_modules}
\title{Remove outlier genes from clusters}
\usage{
tighten_modules(gene_partition, distM, z_thresh = 3)
}
\arguments{
\item{gene_partition}{A named factor of cluster assignments (e.g., output
of \code{\link[=ClusterGenes]{ClusterGenes()}}).}

\item{distM}{A square numeric distance matrix with names matching
\code{names(gene_partition)}.}

\item{z_thresh}{Numeric. Number of MADs above the median to use as the
outlier threshold. Default \code{3}.}
}
\value{
The input \code{gene_partition} factor with outlier genes set to \code{NA}.
}
\description{
For each cluster, computes each member's average distance to other cluster
members and flags as \code{NA} those that are more than \code{z_thresh} MADs above
the median average distance.
}
\keyword{internal}
