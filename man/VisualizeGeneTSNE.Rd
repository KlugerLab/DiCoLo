% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{VisualizeGeneTSNE}
\alias{VisualizeGeneTSNE}
\title{Visualise a gene t-SNE coloured by module or continuous score}
\usage{
VisualizeGeneTSNE(
  dist = NULL,
  gene_embedding = NULL,
  gene_partition = NULL,
  filtered = TRUE,
  perplexity = 30,
  text = TRUE,
  module_color = NULL
)
}
\arguments{
\item{dist}{A \code{dist} object of pairwise gene distances. Required if
\code{gene_embedding} or \code{gene_partition} is NULL.}

\item{gene_embedding}{Data frame with columns \code{tSNE_1} and \code{tSNE_2} (output
of \code{\link[=ObtainGeneTSNE]{ObtainGeneTSNE()}}). If NULL the embedding is computed from \code{dist}.}

\item{gene_partition}{Named factor or character/logical vector of gene module
assignments. If NULL, \code{\link[=ClusterGenes]{ClusterGenes()}} is called on \code{dist}.}

\item{filtered}{Logical. Passed to \code{\link[=ClusterGenes]{ClusterGenes()}} when \code{gene_partition} is
NULL. Default \code{TRUE}.}

\item{perplexity}{Numeric. Passed to \code{\link[=ObtainGeneTSNE]{ObtainGeneTSNE()}} when
\code{gene_embedding} is NULL. Default \code{30}.}

\item{text}{Logical. If \code{TRUE} (default) and \code{gene_partition} is a factor,
module centroid labels are overlaid.}

\item{module_color}{Named character vector mapping module levels to colours.
If NULL, colours are generated automatically from \code{RColorBrewer::brewer.pal}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plots a 2-D t-SNE of genes coloured by a discrete gene partition (modules)
or a continuous numeric score. If no pre-computed embedding or partition is
provided, they are computed internally.
}
\examples{
\dontrun{
p <- VisualizeGeneTSNE(dist = gene_dist, gene_partition = gene_partition)
print(p)
}
}
